# 06-2.1-后端服务检测功能

## 完成时间
2026-05-11

## 关键变更
1. 在 `src/api/index.ts` 中添加了系统检测接口
2. 修正了 `src/page/MaintenancePage.vue` 中的内联样式问题，改为使用 Tailwind CSS

## 实现要点
### API 接口
- 添加 `api.system.healthCheck() 方法
- 调用后端 `/actuator/health/liveness` 接口检测后端状态

### 维护页面
- 使用 Tailwind CSS 实现样式
- 使用 `bg-gradient-to-br` 实现渐变背景
- 使用 `animate-pulse` 实现脉冲动画效果
- 提供"重新检测"按钮触发重试功能

### 应用检测逻辑
- 应用启动时自动检测后端状态
- 后端不可用时显示维护页面
- 支持用户手动重新检测

## 问题解决方案
- 修正了内联样式问题，改用 Tailwind CSS 类
- 移除了内联的 `style` 属性，替换为 `bg-gradient-to-br from-[#1a1a2e] to-[#16213e]`
- 按钮渐变改为 `bg-gradient-to-br from-[#e94560] to-[#c73659]`

## AI 协作记录
- 初始实现时未注意到用户已有 MaintenancePage.vue 位置在 `src/page` 目录
- 修正了重复创建组件的问题
- 修正了内联样式违规问题
- 修正了 API 方法名称与调用不匹配问题

## 验证结果
- TypeScript 类型检查通过
- 符合 Vue 3 Composition API 规范
- 符合 Tailwind CSS 样式规范
